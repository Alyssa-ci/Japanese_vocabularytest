# 快速开始指南

## 📋 第一步：检查环境

### 方法一：使用检查脚本
双击运行 `检查环境.bat` 文件，会自动检查 Node.js 和 npm 是否已安装。

### 方法二：手动检查
在 Cursor 终端中（按 `Ctrl + `` 打开），执行以下命令：

```bash
node --version
npm --version
```

**如果显示版本号**（如 `v18.17.0` 和 `9.6.7`），说明已安装，可以继续下一步。

**如果显示错误**，需要先安装 Node.js：
- 访问：https://nodejs.org/
- 下载并安装 LTS 版本（推荐）
- 安装完成后重启 Cursor

---

## 🚀 第二步：安装依赖和启动

### 方法一：使用批处理文件（最简单）
1. 双击运行 `install-and-run.bat`
2. 等待安装完成（首次可能需要几分钟）
3. 看到 "Ready" 提示后，在浏览器打开 `http://localhost:3000`

### 方法二：在 Cursor 终端中手动执行

1. **打开终端**：按 `Ctrl + `` (反引号)

2. **安装依赖**：
   ```bash
   npm install
   ```
   等待安装完成（可能需要几分钟）

3. **启动开发服务器**：
   ```bash
   npm run dev
   ```

4. **访问网站**：
   - 终端会显示：`Local: http://localhost:3000`
   - 在浏览器中打开该地址

---

## ⚠️ 常见问题

### 问题1：npm 命令不存在
**解决方案**：
- 安装 Node.js：https://nodejs.org/
- 安装后重启 Cursor
- 重新打开终端检查

### 问题2：安装很慢或失败
**解决方案**：使用国内镜像源
```bash
npm install --registry=https://registry.npmmirror.com
```

### 问题3：端口 3000 被占用
**解决方案**：
- Next.js 会自动使用其他端口（如 3001）
- 查看终端输出的实际地址

### 问题4：依赖安装失败
**解决方案**：
1. 删除 `node_modules` 文件夹（如果存在）
2. 删除 `package-lock.json` 文件（如果存在）
3. 重新运行 `npm install`

---

## 📝 项目结构说明

```
测试词汇量/
├── app/                    # Next.js 应用目录
│   ├── page.tsx           # 首页
│   ├── quiz/              # 测评相关页面
│   └── report/            # 报告页面
├── lib/                   # 工具函数和类型定义
├── package.json          # 项目依赖配置
└── install-and-run.bat   # 一键启动脚本
```

---

## 🎯 下一步

安装并启动成功后：
1. 访问首页查看网站
2. 点击"开始测评"体验完整流程
3. 查看生成的测评报告

祝使用愉快！🎉
