# 启动说明

## 方法一：使用批处理文件（推荐）

1. 双击运行 `install-and-run.bat` 文件
2. 等待依赖安装完成（首次可能需要几分钟）
3. 服务器启动后，在浏览器中打开 `http://localhost:3000`

## 方法二：手动在终端中执行

### 在 Cursor 终端中：

1. 按 `Ctrl + `` (反引号) 打开终端
2. 执行以下命令：

```bash
npm install
```

3. 安装完成后，执行：

```bash
npm run dev
```

4. 在浏览器中访问：`http://localhost:3000`

### 在 Windows PowerShell 或 CMD 中：

1. 打开 PowerShell 或 CMD
2. 进入项目目录：
   ```powershell
   cd "c:\Users\77132\Desktop\测试词汇量"
   ```
3. 安装依赖：
   ```bash
   npm install
   ```
4. 启动服务器：
   ```bash
   npm run dev
   ```

## 注意事项

- 确保已安装 Node.js（版本 18 或更高）
- 首次安装依赖需要网络连接，可能需要几分钟时间
- 服务器启动后，终端会显示访问地址
- 按 `Ctrl + C` 可以停止开发服务器

## 如果遇到问题

1. **npm 命令不存在**：需要先安装 Node.js
   - 访问 https://nodejs.org/ 下载安装

2. **安装失败**：检查网络连接或使用国内镜像
   ```bash
   npm install --registry=https://registry.npmmirror.com
   ```

3. **端口被占用**：如果 3000 端口被占用，Next.js 会自动使用其他端口
